import { Meta, IconGallery, IconItem } from '@storybook/blocks';
import { icons } from './Iconography';

<Meta title="Systems/Iconography" />

## Iconography

Here are all the SVG icons available in the `public/icons` directory.

<IconGallery>
    {icons.map(({ Icon, title }) => (
        <IconItem name={title} key={`icon-${title}`}>
            <Icon />
        </IconItem>
    ))}
</IconGallery>
